import { createUseStyles } from 'react-jss';

export const useStyles = createUseStyles({
  widget: {
    height: 'inherit',
    width: 'inherit',
    background: '#fff',
    borderRadius: 32,
    position: 'relative',
  },

  widgetContainer: {
    padding: 32,
  }
})
