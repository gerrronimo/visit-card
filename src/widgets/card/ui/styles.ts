import { createUseStyles } from 'react-jss';

export const useStyles = createUseStyles({
    card: {
        width: '100%',
        height: '100%',
        background: '#fff',
        border: '1px solid #F6F7F6',
        borderRadius: 36,
        marginBottom: 16,
    },
});
